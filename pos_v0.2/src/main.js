//TODO: Please write code in this file.
function printInventory(inputs) {
    var allitem = loadAllItems();
    var item = new determine(), bbox;
    bbox = item.getbbox(inputs,allitem);
    prints(bbox);
}









